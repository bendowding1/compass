# Normalize line endings: store text as LF in the repo and check out as LF
# everywhere, so Windows/Mac/Linux contributors share identical files.
* text=auto eol=lf

# Common binary assets — never normalize.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.webp binary
*.woff binary
*.woff2 binary
*.pdf binary
